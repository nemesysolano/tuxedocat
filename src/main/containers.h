#ifndef __CONTAINERS__
#define __CONTAINERS__
#include <map>
#include <vector>
#include <string>

using namespace std;

namespace containers {
    template <typename T> class MultivalueDictionary : public map<string, 
};

#endif